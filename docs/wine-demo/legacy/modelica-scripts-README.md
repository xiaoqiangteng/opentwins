# Modelica Scripts

- `run_hello_wine.mos` runs the standalone `WineFermentation.HelloWine` smoke model.
- `run_tank_prediction.mos.template` is rendered by the Simulation Service for tank continuation simulations.

Use `../scripts/run_modelica_demo.sh` from the project root for the repeatable Docker-based smoke test.
